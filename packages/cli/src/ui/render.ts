export function makeUiData({ projectRoot, topology, frontendRequests, backendRoutes, result }: any) {
  const diagnostics = result.diagnostics.map((d: any, i: number) => ({
    id: `d${i}`,
    code: d.code,
    severity: d.severity,
    message: d.message,
    location: d.location,
    method: d.frontend?.method ?? d.backend?.method ?? null,
    path: d.frontend?.normalizedPath ?? d.backend?.normalizedPath ?? d.backend?.rawPath ?? d.frontend?.rawUrl ?? null,
    frontend: d.frontend
      ? { method: d.frontend.method, path: d.frontend.normalizedPath ?? d.frontend.rawUrl, location: d.frontend.location }
      : null,
    backend: d.backend
      ? {
          method: d.backend.method,
          path: d.backend.normalizedPath ?? d.backend.rawPath,
          handler: d.backend.handler ?? null,
          location: d.backend.location,
        }
      : null,
  }));
  const matches = result.matches.map((m: any, i: number) => ({
    id: `m${i}`,
    method: m.frontend.method,
    path: m.frontend.normalizedPath,
    frontend: m.frontend.location,
    backend: m.backend.location,
    handler: m.backend.handler ?? null,
  }));
  const requests = frontendRequests.map((r: any, i: number) => ({
    id: `f${i}`,
    method: r.method,
    path: r.normalizedPath ?? r.rawUrl,
    rawUrl: r.rawUrl,
    location: r.location,
    resolved: !!r.normalizedPath && !!r.method,
  }));
  const routes = backendRoutes.map((r: any, i: number) => ({
    id: `b${i}`,
    method: r.method,
    path: r.normalizedPath ?? r.rawPath,
    rawPath: r.rawPath,
    handler: r.handler ?? null,
    location: r.location,
  }));
  return {
    projectRoot,
    topology,
    requests,
    routes,
    summary: {
      frontendRequests: requests.length,
      backendRoutes: routes.length,
      matched: matches.length,
      errors: diagnostics.filter((d: any) => d.severity === "error").length,
      warnings: diagnostics.filter((d: any) => d.severity === "warning").length,
    },
    matches,
    diagnostics,
  };
}

export function renderUi(data: any): string {
  const payload = JSON.stringify(data).replaceAll("<", "\\u003c");
  const topology = [...data.topology.frontends, ...data.topology.backends];
  return `<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>AWA Visual Explorer</title><style>
:root{font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color-scheme:dark;--bg:#080d13;--side:#0c121a;--panel:#101822;--panel2:#131e2a;--line:#223142;--muted:#8496ab;--text:#edf4fc;--green:#43d890;--red:#ff6868;--amber:#f0b84d;--blue:#42a5ff;--purple:#a781ff;--cyan:#2bd0df}*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at 70% -20%,#152638 0,transparent 35%),var(--bg);color:var(--text)}button,input{font:inherit}.app{display:grid;grid-template-columns:238px minmax(0,1fr);min-height:100vh;transition:grid-template-columns .18s ease}.app.sideCollapsed{grid-template-columns:0 minmax(0,1fr)}.side{border-right:1px solid var(--line);background:linear-gradient(180deg,#0d141d,#0a1017);padding:22px 12px;position:sticky;top:0;height:100vh;z-index:8;overflow:hidden;transition:opacity .15s ease,padding .18s ease}.app.sideCollapsed .side{opacity:0;padding-left:0;padding-right:0;border-right:0;pointer-events:none}.brand{font-size:24px;font-weight:850;letter-spacing:-.05em;padding:0 10px}.brand span{color:var(--cyan)}.brand small{display:block;color:#6f839b;font-size:10px;letter-spacing:.14em;margin-top:3px}.nav{margin-top:28px;display:grid;gap:18px}.navGroup{display:grid;gap:4px}.navLabel{padding:0 12px 5px;color:#60758c;font-size:10px;font-weight:700;letter-spacing:.12em;text-transform:uppercase}.nav button{width:100%;border:0;background:transparent;color:#9cb0c6;text-align:left;padding:10px 12px;border-radius:9px;cursor:pointer;transition:.15s;display:flex;align-items:center;gap:9px}.nav button:hover{background:#131e2a;color:#fff}.nav button.active{background:linear-gradient(90deg,#15365c,#162536);color:#fff;box-shadow:inset 3px 0 var(--blue)}.ico{width:18px;text-align:center;color:#6f91b6}.count{margin-left:auto;background:#243447;color:#c8d7e8;border-radius:20px;padding:1px 7px;font-size:10px}.sidefoot{position:absolute;bottom:18px;left:18px;right:18px;border:1px solid var(--line);background:#0f1822;border-radius:10px;padding:11px;color:#70849b;font-size:10px;line-height:1.5}.main{min-width:0}.topLeft{display:flex;align-items:center;gap:10px;min-width:0}.top{display:flex;justify-content:space-between;align-items:center;background:#0a0f18;border-bottom:1px solid var(--line);padding:14px 23px;position:sticky;top:0;z-index:7}.sideToggle{background:transparent;border:0;width:32px;height:32px;border-radius:8px;color:inherit;cursor:pointer;display:grid;place-content:center;margin:-4px -4px -4px 0}.sideToggle:hover{background:var(--line)}.project{font-weight:600;font-size:14px;color:var(--muted)}.status{font-size:12px;color:var(--muted)}.content{padding:23px}.title{font-size:32px;font-weight:850;margin:0 0 6px 0;letter-spacing:-.02em}.sub{color:var(--muted);font-size:14px;margin:0 0 23px 0}.cards{display:grid;grid-template-columns:repeat(5,1fr);gap:12px;margin-bottom:23px}.card{background:var(--panel);border:1px solid var(--line);border-radius:10px;padding:19px;text-align:center}.metric{font-size:32px;font-weight:850;line-height:1;margin:0 0 9px}.metric.good{color:var(--green)}.metric.bad{color:var(--red)}.metric.warn{color:var(--amber)}.label{font-size:11px;color:var(--muted);letter-spacing:.05em;text-transform:uppercase}.grid{display:grid;grid-template-columns:repeat(2,1fr);gap:19px;margin-bottom:19px}.panel{background:var(--panel);border:1px solid var(--line);border-radius:10px;padding:18px}.panel h2{font-size:14px;font-weight:700;margin:0 0 14px 0;text-transform:uppercase;letter-spacing:.08em;color:#c8d7e8}.chips{display:flex;flex-wrap:wrap;gap:8px}.chip{background:var(--panel2);border:1px solid var(--line);border-radius:6px;padding:6px 11px;font-size:12px;white-space:nowrap}.empty{color:var(--muted);text-align:center;padding:32px 23px}.hint{margin-top:12px;background:var(--panel2);border-left:3px solid var(--blue);border-radius:4px;padding:11px 14px;font-size:12px;color:var(--muted);line-height:1.5}.muted{color:var(--muted)}.toolbar{display:flex;gap:12px;margin-bottom:16px}.search{flex:1;background:var(--panel);border:1px solid var(--line);border-radius:8px;padding:9px 14px;font-size:13px;color:inherit;min-width:0}.seg{display:flex;gap:4px}.seg button{border:0;background:transparent;color:var(--muted);padding:7px 11px;border-radius:6px;cursor:pointer;font-size:12px;transition:.15s}.seg button:hover{background:var(--line);color:inherit}.seg button.active{background:var(--blue);color:#000;font-weight:600}.toolbtn{border:0;background:transparent;color:var(--muted);width:32px;height:32px;border-radius:6px;cursor:pointer;transition:.15s;font-size:13px}.toolbtn:hover{background:var(--line);color:inherit}.rows{display:grid;gap:1px;background:var(--line);border-radius:8px;overflow:hidden;border:1px solid var(--line)}.row{display:grid;grid-template-columns:70px 1fr 1fr 150px;gap:13px;background:var(--panel);padding:12px 16px;font-size:12px;align-items:center}.row:hover{background:var(--panel2)}.row.frontendRow,.row.backendRow,.row.issueRow,.row.searchRow{cursor:pointer}.badge{background:var(--blue);color:#000;border-radius:4px;padding:2px 7px;font-size:10px;font-weight:700;width:max-content;text-transform:uppercase}.badge.warning{background:var(--amber)}.badge.error{background:var(--red)}.method{font-weight:600;font-family:Menlo,monospace;color:var(--text)}.path{font-family:Menlo,monospace;word-break:break-all}.detail{position:fixed;right:0;top:0;bottom:0;width:380px;background:#0a0f18;border-left:1px solid var(--line);box-shadow:-8px 0 24px rgba(0,0,0,.5);transform:translateX(100%);transition:transform .25s ease;z-index:10}.detail.open{transform:translateX(0)}.close{position:absolute;top:14px;right:14px;width:32px;height:32px;border:0;background:transparent;color:inherit;cursor:pointer;border-radius:6px}.close:hover{background:var(--line)}.detail h2{font-size:13px;font-weight:700;margin:49px 18px 11px;text-transform:uppercase;letter-spacing:.08em;color:#9cb0c6}.detail pre{font-size:11px;line-height:1.6;margin:0 18px 18px;padding:11px;background:var(--panel2);border-radius:6px;overflow:auto;overflow-wrap:anywhere;word-break:break-word;white-space:pre-wrap;max-height:70vh;font-family:Menlo,monospace}.graphShell{background:var(--panel);border:1px solid var(--line);border-radius:10px;padding:19px;overflow:auto}.graphLegend{display:flex;gap:16px;flex-wrap:wrap;margin-bottom:14px;font-size:12px}.dot{display:inline-block;width:8px;height:8px;border-radius:50%;margin-right:6px}.graph{width:100%;height:100%}.graph svg{display:block}.laneBox{fill:var(--panel2);opacity:.4}.laneTitle{font-size:11px;font-weight:700;letter-spacing:.08em;text-transform:uppercase;fill:var(--muted)}.nodeTitle{font-size:12px;font-weight:600;fill:var(--text)}.nodeMeta{font-size:10px;fill:var(--muted)}.edge{fill:none;stroke-width:2;stroke-linecap:round;stroke-linejoin:round}.edge.verified{stroke:var(--green)}.edge.error{stroke:var(--red)}.edge.warning{stroke:var(--amber)}.graphEmpty{text-align:center;padding:48px 32px;color:var(--muted)}.help pre{background:var(--panel2);border:1px solid var(--line);border-radius:8px;padding:14px;overflow-x:auto;font-size:12px;line-height:1.4;margin-bottom:19px}.help .kbd{background:var(--panel);border:1px solid var(--line);border-radius:4px;padding:2px 6px;font-family:Menlo,monospace;font-size:11px}@media(max-width:900px){.app,.app.sideCollapsed{grid-template-columns:1fr}.side{height:auto;position:static;border-right:0;border-bottom:1px solid var(--line)}.sidefoot{display:none}.nav{display:flex;margin-top:14px;overflow:auto}.nav button{width:auto;min-width:max-content}.cards{grid-template-columns:repeat(2,1fr)}.grid{grid-template-columns:1fr}.content{padding:19px}.top{padding:0 19px}.row{grid-template-columns:70px 1fr}.row>*:nth-child(3),.row>*:nth-child(4){display:none}}
</style></head><body><div id="appShell" class="app"><aside class="side"><div class="brand"><span>A</span>WA<small>APPLICATION WIRING ANALYZER</small></div><nav class="nav" aria-label="Explorer"><div class="navGroup"><div class="navLabel">Explore</div><button type="button" data-view="overview" class="active"><span class="ico">⌂</span>Overview</button><button type="button" data-view="wiring"><span class="ico">⌘</span>Wiring Map <span class="count">${data.matches.length}</span></button></div><div class="navGroup"><div class="navLabel">Application</div><button type="button" data-view="frontend"><span class="ico">◫</span>Frontend <span class="count">${data.requests.length}</span></button><button type="button" data-view="backend"><span class="ico">▤</span>Backend <span class="count">${data.routes.length}</span></button><button type="button" data-view="issues"><span class="ico">△</span>Issues <span class="count">${data.diagnostics.length}</span></button></div><div class="navGroup"><div class="navLabel">Tools</div><button type="button" data-view="search"><span class="ico">⌕</span>Search</button><button type="button" data-view="help"><span class="ico">?</span>Run / Help</button></div></nav></aside><main class="main"><header class="top"><div class="topLeft"><button id="sideToggle" class="sideToggle" type="button" title="Hide sidebar" aria-label="Hide sidebar" aria-expanded="true">☰</button><div class="project">${esc(data.projectRoot)}</div></div><div class="status">● Analysis complete</div></header><div class="content">
<section id="overview" class="view"><h1 class="title">Application overview</h1><p class="sub">What AWA detected and verified in this repository.</p><div class="cards"><div class="card"><div class="metric">${data.summary.frontendRequests}</div><div class="label">Frontend requests</div></div><div class="card"><div class="metric">${data.summary.backendRoutes}</div><div class="label">Backend routes</div></div><div class="card"><div class="metric good">${data.summary.matched}</div><div class="label">Verified connections</div></div><div class="card"><div class="metric bad">${data.summary.errors}</div><div class="label">Errors</div></div><div class="card"><div class="metric warn">${data.summary.warnings}</div><div class="label">Warnings</div></div></div><div class="grid"><div class="panel"><h2>Detected application topology</h2><div class="chips">${topology.map((x: any) => `<div class="chip">${esc(x.kind)} · <b>${esc(x.framework)}</b> · ${esc(x.language)} · ${esc(x.root)}</div>`).join("") || '<div class="muted">No framework roots confidently identified.</div>'}</div></div><div class="panel"><h2>Languages</h2><div class="chips">${data.topology.languages.map((x: any) => `<div class="chip"><b>${esc(x.language)}</b> · ${x.files} files</div>`).join("")}</div></div></div><div class="panel" style="margin-top:13px"><h2>Wiring health</h2><div class="hint">Use <b>Wiring Map</b> for the visual frontend → HTTP contract → backend graph. Select any node to inspect exact source evidence.</div></div></section>
<section id="wiring" class="view" hidden><h1 class="title">Wiring map</h1><p class="sub">A visual map of verified, broken and uncertain application connections.</p><div class="toolbar"><input id="wireSearch" class="search" placeholder="Filter route, handler or source file…"><div class="seg" id="wireFilter"><button type="button" data-filter="all" class="active">All</button><button type="button" data-filter="verified">Verified</button><button type="button" data-filter="error">Broken</button><button type="button" data-filter="warning">Uncertain</button></div><button class="toolbtn" id="zoomOut">−</button><button class="toolbtn" id="zoomReset">100%</button><button class="toolbtn" id="zoomIn">+</button></div><div class="graphShell"><div class="graphLegend"><span><i class="dot" style="background:var(--blue)"></i>Frontend</span><span><i class="dot" style="background:var(--purple)"></i>HTTP contract</span><span><i class="dot" style="background:var(--green)"></i>Backend</span><span><i class="dot" style="background:var(--red)"></i>Broken</span><span><i class="dot" style="background:var(--amber)"></i>Uncertain</span></div><div id="graph"></div></div><div class="hint">Large repositories are capped at 120 visible relationships. Search/filter narrows the graph without changing analysis.</div></section>
<section id="frontend" class="view" hidden><h1 class="title">Frontend requests</h1><p class="sub">HTTP calls discovered from frontend/application source.</p><div class="toolbar"><input id="frontendSearch" class="search" placeholder="Search method, path or source…"></div><div id="frontendRows" class="rows"></div></section>
<section id="backend" class="view" hidden><h1 class="title">Backend routes</h1><p class="sub">Routes discovered from supported backend adapters.</p><div class="toolbar"><input id="backendSearch" class="search" placeholder="Search method, route, handler or source…"></div><div id="backendRows" class="rows"></div></section>
<section id="issues" class="view" hidden><h1 class="title">Issues</h1><p class="sub">Broken or uncertain wiring backed by exact source locations.</p><div class="toolbar"><input id="issueSearch" class="search" placeholder="Filter code, route or source file…"></div><div id="issueRows" class="rows"></div></section>
<section id="search" class="view" hidden><h1 class="title">Search application wiring</h1><p class="sub">Search requests, routes, handlers, files and diagnostics together.</p><div class="toolbar"><input id="globalSearch" class="search" placeholder="Type a route, file, handler, diagnostic code…" autofocus></div><div id="searchRows" class="rows"><div class="empty">Start typing to search the analyzed application.</div></div></section>
<section id="help" class="view help" hidden><h1 class="title">Run AWA</h1><p class="sub">The Visual Explorer and CLI use the same local deterministic analysis.</p><div class="panel"><h2>1. Install and build</h2><pre><code>npm install\nnpm run build</code></pre><h2>2. Open the Visual Explorer</h2><pre><code>awa .</code></pre><p class="hint">To analyze another repository, pass its path:</p><pre><code>awa /path/to/project</code></pre><h2>3. CLI / CI verification</h2><pre><code>awa check /path/to/project</code></pre><p class="hint">AWA detects languages and application topology automatically, then analyzes the supported frontend and backend wiring it finds.</p><h2>Browser address</h2><pre><code>http://127.0.0.1:4177</code></pre></div></section>
</div></main></div><aside id="detail" class="detail" aria-hidden="true"><button id="closeDetail" class="close">Close</button><h2 id="detailTitle">Details</h2><pre id="detailBody"></pre></aside>
<script>const DATA=${payload};
const byId=id=>document.getElementById(id);let wireFilter='all',zoom=1;
function toggleSidebar(){const shell=byId('appShell'),btn=byId('sideToggle');const collapsed=shell.classList.toggle('sideCollapsed');btn.setAttribute('aria-expanded',String(!collapsed));btn.setAttribute('aria-label',collapsed?'Show sidebar':'Hide sidebar');btn.title=collapsed?'Show sidebar':'Hide sidebar'}
byId('sideToggle').addEventListener('click',toggleSidebar);
window.addEventListener('pageshow',e=>{if(e.persisted)location.reload()});
function eh(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function loc(x){return x?x.file+':'+x.line+':'+x.column:'—'}function shortFile(f){const p=String(f||'').replaceAll('\\\\','/').split('/');return p.slice(-2).join('/')}function trunc(s,n){s=String(s??'');return s.length>n?s.slice(0,n-1)+'…':s}
function activateView(name,push){const valid=['overview','wiring','frontend','backend','issues','search','help'];if(!valid.includes(name))name='overview';closeDetail();document.querySelectorAll('.view').forEach(v=>v.hidden=v.id!==name);document.querySelectorAll('[data-view]').forEach(b=>b.classList.toggle('active',b.dataset.view===name));if(push!==false&&location.hash!=='#'+name)history.pushState(null,'','#'+name);if(name==='wiring')renderGraph();if(name==='frontend')renderFrontend();if(name==='backend')renderBackend();if(name==='issues')renderIssues();if(name==='search')setTimeout(()=>byId('globalSearch').focus(),0)}
document.querySelectorAll('[data-view]').forEach(b=>b.addEventListener('click',()=>activateView(b.dataset.view,true)));window.addEventListener('hashchange',()=>activateView(location.hash.slice(1),false));
function graphItems(){const q=byId('wireSearch').value.trim().toLowerCase();const verified=DATA.matches.map(x=>({kind:'verified',...x}));const issues=DATA.diagnostics.map(x=>({kind:x.severity==='error'?'error':'warning',...x,frontend:x.frontend?.location||null,backend:x.backend?.location||null,handler:x.backend?.handler||null}));return [...verified,...issues].filter(x=>(wireFilter==='all'||x.kind===wireFilter)&&(!q||JSON.stringify(x).toLowerCase().includes(q))).slice(0,120)}
function edge(x1,y1,x2,y2,status){const c=(x2-x1)*.52;return '<path class="edge '+status+'" d="M '+x1+' '+y1+' C '+(x1+c)+' '+y1+', '+(x2-c)+' '+y2+', '+x2+' '+y2+'"/>'}
function node(x,y,w,h,title,meta,type,id){const fills={frontend:'#10233a',api:'#211b38',backend:'#10271e',broken:'#2b171b',ghost:'#252116'},strokes={frontend:'#2c6da7',api:'#7057a8',backend:'#2b7653',broken:'#a33d44',ghost:'#7b6530'};return '<g class="graphNode" data-id="'+eh(id)+'"><rect x="'+x+'" y="'+y+'" width="'+w+'" height="'+h+'" rx="9" fill="'+fills[type]+'" stroke="'+strokes[type]+'"/><text class="nodeTitle" x="'+(x+13)+'" y="'+(y+23)+'">'+eh(trunc(title,36))+'</text><text class="nodeMeta" x="'+(x+13)+'" y="'+(y+42)+'">'+eh(trunc(meta,43))+'</text></g>'}
function renderGraph(){const items=graphItems();if(!items.length){byId('graph').innerHTML='<div class="graphEmpty">No relationships match this filter.</div>';return}const rowH=84,top=66,width=1240,height=top+items.length*rowH+32;let p=['<svg width="'+(width*zoom)+'" height="'+(height*zoom)+'" viewBox="0 0 '+width+' '+height+'" xmlns="http://www.w3.org/2000/svg">','<rect class="laneBox" x="34" y="42" width="330" height="'+(height-58)+'" rx="14"/><rect class="laneBox" x="455" y="42" width="330" height="'+(height-58)+'" rx="14"/><rect class="laneBox" x="876" y="42" width="330" height="'+(height-58)+'" rx="14"/>','<text class="laneTitle" x="52" y="28">FRONTEND</text><text class="laneTitle" x="473" y="28">HTTP REQUEST / CONTRACT</text><text class="laneTitle" x="894" y="28">BACKEND</text>'];items.forEach((x,i)=>{const y=top+i*rowH,cy=y+27,status=x.kind,fe=x.frontend,be=x.backend,api=(x.code?x.code+' · ':'')+(x.method||'?')+' '+(x.path||'unresolved');if(fe){p.push(edge(342,cy,477,cy,status));p.push(node(52,y,290,54,shortFile(fe.file),loc(fe),'frontend',x.id))}else{p.push(edge(342,cy,477,cy,status));p.push(node(52,y,290,54,'No detected frontend caller','May be external / server-to-server','ghost',x.id))}p.push(node(477,y,286,54,api,x.message||'HTTP contract','api',x.id));p.push(edge(763,cy,898,cy,status));if(be)p.push(node(898,y,286,54,x.handler||'Backend route',loc(be),'backend',x.id));else p.push(node(898,y,286,54,'No matching backend route','Broken connection','broken',x.id))});p.push('</svg>');byId('graph').innerHTML=p.join('');document.querySelectorAll('.graphNode').forEach(n=>n.addEventListener('click',()=>showAny(n.dataset.id)))}
function genericRows(items,type){if(!items.length)return '<div class="empty">No results.</div>';return items.map(x=>'<div class="row '+type+'Row" data-id="'+eh(x.id)+'"><span class="method">'+eh(x.method||'?')+'</span><span class="path">'+eh(x.path||'unresolved')+'</span><span class="muted">'+eh(type==='backend'?(x.handler||'—'):loc(x.location))+'</span><span class="muted">'+eh(type==='backend'?loc(x.location):(x.resolved?'Resolved':'Unresolved'))+'</span></div>').join('')}
function renderFrontend(){const q=byId('frontendSearch').value.toLowerCase();const a=DATA.requests.filter(x=>JSON.stringify(x).toLowerCase().includes(q));byId('frontendRows').innerHTML=genericRows(a,'frontend');document.querySelectorAll('.frontendRow').forEach(r=>r.addEventListener('click',()=>showSource('Frontend request',DATA.requests.find(x=>x.id===r.dataset.id))))}
function renderBackend(){const q=byId('backendSearch').value.toLowerCase();const a=DATA.routes.filter(x=>JSON.stringify(x).toLowerCase().includes(q));byId('backendRows').innerHTML=genericRows(a,'backend');document.querySelectorAll('.backendRow').forEach(r=>r.addEventListener('click',()=>showSource('Backend route',DATA.routes.find(x=>x.id===r.dataset.id))))}
function renderIssues(){const q=byId('issueSearch').value.toLowerCase();const a=DATA.diagnostics.filter(x=>JSON.stringify(x).toLowerCase().includes(q));byId('issueRows').innerHTML=a.length?a.map(x=>'<div class="row issueRow" data-id="'+x.id+'"><span class="badge '+x.severity+'">'+eh(x.code)+'</span><span class="path">'+eh((x.method||'')+' '+(x.path||x.message))+'</span><span class="muted">'+eh(x.message)+'</span><span class="muted">'+eh(loc(x.location))+'</span></div>').join(''):'<div class="empty">No issues found.</div>';document.querySelectorAll('.issueRow').forEach(r=>r.addEventListener('click',()=>showAny(r.dataset.id)))}
function renderSearch(){const q=byId('globalSearch').value.trim().toLowerCase();if(!q){byId('searchRows').innerHTML='<div class="empty">Start typing to search the analyzed application.</div>';return}const hits=[];DATA.requests.filter(x=>JSON.stringify(x).toLowerCase().includes(q)).slice(0,30).forEach(x=>hits.push({kind:'Frontend',id:x.id,title:(x.method||'?')+' '+x.path,meta:loc(x.location),obj:x}));DATA.routes.filter(x=>JSON.stringify(x).toLowerCase().includes(q)).slice(0,30).forEach(x=>hits.push({kind:'Backend',id:x.id,title:x.method+' '+x.path,meta:(x.handler||'—')+' · '+loc(x.location),obj:x}));DATA.diagnostics.filter(x=>JSON.stringify(x).toLowerCase().includes(q)).slice(0,30).forEach(x=>hits.push({kind:x.code,id:x.id,title:(x.method||'')+' '+(x.path||''),meta:x.message,obj:x,diag:true}));byId('searchRows').innerHTML=hits.length?hits.map((h,i)=>'<div class="row searchRow" data-i="'+i+'"><span class="badge">'+eh(h.kind)+'</span><span class="path">'+eh(h.title)+'</span><span class="muted">'+eh(h.meta)+'</span><span class="muted">Inspect</span></div>').join(''):'<div class="empty">No matches.</div>';document.querySelectorAll('.searchRow').forEach(r=>r.addEventListener('click',()=>{const h=hits[Number(r.dataset.i)];h.diag?showAny(h.id):showSource(h.kind,h.obj)}))}
function showSource(title,x){if(!x)return;byId('detailTitle').textContent=title;byId('detailBody').textContent=[(x.method||'?')+' '+(x.path||'unresolved'),'','Source','  '+loc(x.location),x.handler?'':'',x.handler?'Handler\\n  '+x.handler:''].filter(Boolean).join('\\n');openDetail()}
function showAny(id){let x=DATA.matches.find(v=>v.id===id);if(x){byId('detailTitle').textContent='Verified wiring';byId('detailBody').textContent=[x.method+' '+x.path,'','Frontend','  '+loc(x.frontend),'','Backend','  '+loc(x.backend),'','Handler','  '+(x.handler||'—'),'','Status','  Verified frontend → backend connection'].join('\\n');openDetail();return}x=DATA.diagnostics.find(v=>v.id===id);if(!x)return;byId('detailTitle').textContent=x.code+' · '+x.severity;byId('detailBody').textContent=[x.message,'','Primary location','  '+loc(x.location),x.frontend?'\\nFrontend\\n  '+loc(x.frontend.location):'',x.backend?'\\nBackend\\n  '+loc(x.backend.location)+(x.backend.handler?'\\n  Handler: '+x.backend.handler:''):''].filter(Boolean).join('\\n');openDetail()}
function openDetail(){byId('detail').classList.add('open');byId('detail').setAttribute('aria-hidden','false')}function closeDetail(){byId('detail').classList.remove('open');byId('detail').setAttribute('aria-hidden','true')}
byId('closeDetail').addEventListener('click',closeDetail);document.addEventListener('click',e=>{const target=e.target;if(!(target instanceof Element))return;const detail=byId('detail');if(detail.classList.contains('open')&&!detail.contains(target)&&!target.closest('.row,.graphNode'))closeDetail()});document.addEventListener('keydown',e=>{if(e.key==='Escape')closeDetail()});byId('wireSearch').addEventListener('input',renderGraph);byId('frontendSearch').addEventListener('input',renderFrontend);byId('backendSearch').addEventListener('input',renderBackend);byId('issueSearch').addEventListener('input',renderIssues);byId('globalSearch').addEventListener('input',renderSearch);document.querySelectorAll('#wireFilter button').forEach(b=>b.addEventListener('click',()=>{wireFilter=b.dataset.filter;document.querySelectorAll('#wireFilter button').forEach(x=>x.classList.toggle('active',x===b));renderGraph()}));function setZoom(v){zoom=Math.max(.65,Math.min(1.55,v));byId('zoomReset').textContent=Math.round(zoom*100)+'%';renderGraph()}byId('zoomIn').addEventListener('click',()=>setZoom(zoom+.1));byId('zoomOut').addEventListener('click',()=>setZoom(zoom-.1));byId('zoomReset').addEventListener('click',()=>setZoom(1));renderFrontend();renderBackend();renderIssues();activateView(location.hash.slice(1)||'overview',false);
</script></body></html>`;
}

function esc(value: any): string {
  return String(value)
    .replace(/[&<>"']/g, (c) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }[c] || c));
}
